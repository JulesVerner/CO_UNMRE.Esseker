if !(hasInterface) exitWith {};

private _blackFade = true;

// dont run twice in SP
if (!isServer) then {
    [] call compile preprocessFileLineNumbers "USER\userIntro.sqf";
};

[{!isNil "GRAD_introCam_playOnStart"}, 
{
    params ["_blackFade"];
    if (
        GRAD_introCam_playOnStart &&
        {_blackFade}
    ) then {
        cutText ["", "BLACK FADED", 999];
        0 fadeSound 0;
    };
},[_blackFade]] call CBA_fnc_waitUntilAndExecute;